{
    "labels": [
        {
            "type": "unshared",
            "query": {
                "start": "@critical_work/call[func=pthread_mutex_lock]",
                "end": "@critical_work/call[func=pthread_mutex_unlock]"
            },
            "magic_vars": ["mutex"],
            "comment": "Critical section in critical_work - marks mutex hold duration for priority inversion prevention"
        }
    ]
}
